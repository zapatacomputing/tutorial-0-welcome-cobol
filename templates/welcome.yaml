spec:
  templates:

  - name: welcome-to-orquestra
    parent: generic-task
    inputs:
      parameters:
      - name: command
        value: ./welcome
      - name: docker-image
        default: welcome-cobol
      artifacts:
    outputs:
      artifacts:
      - name: welcome
        path: /app/welcome.json